---
description: "Set fixVersions on resolved Jira tickets by tracing merged PR branches. Use when the user asks to set fix version, assign fix version, find tickets missing fixVersions, or run jira-fix-version."
alwaysApply: false
---

# jira-fix-version

Read `.claude/agents/jira-fix-version.md` and follow the complete workflow described there.

The agent spec contains all steps, MCP tool call examples, CSV schemas, validation checkpoints, classification rules, and rules. It is the single source of truth for this workflow.

Ignore the YAML frontmatter fields `model` and `memory` — those are Claude Code-specific and have no effect in Cursor.

For best results, use Claude Opus or an equivalent model in Cursor settings.
